// Generated by CoffeeScript 1.12.6
(function() {
  module.exports = function(sql, nextBindFn) {
    var c, i, length, newSql, ref, startQuote;
    newSql = '';
    startQuote = false;
    i = 0;
    length = sql.length;
    while (i < length) {
      c = sql[i];
      i++;
      if (startQuote !== false) {
        if (c === '\\' || (c === startQuote && sql[i] === startQuote)) {
          newSql += c;
          c = (ref = sql[i]) != null ? ref : '';
          i++;
        } else if (c === startQuote) {
          startQuote = false;
        }
        newSql += c;
      } else if (c === "'" || c === '"') {
        startQuote = c;
        newSql += c;
      } else if (c === '?') {
        newSql += nextBindFn();
      } else {
        newSql += c;
      }
    }
    return newSql;
  };

}).call(this);

//# sourceMappingURL=sql-binds.js.map
